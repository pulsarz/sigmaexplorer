@page "/token/{tokenID}"
@page "/en/token/{tokenID}"
@page "/token/byid/{tokenID}"
@using SigmaExplorer.Shared

<PageTitle>Token detail</PageTitle>
<ErgoTokenDetail TokenID=@tokenID></ErgoTokenDetail>

@code {
    [Parameter]
    public string? tokenID { get; set; }

    protected override async Task OnParametersSetAsync()
    {
    }
}
